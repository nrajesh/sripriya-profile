/** @type {import('next').NextConfig} */
const nextConfig = {
  // If you were using external images, you would configure domains here.
  // Since we are using local images, this configuration is minimal.
};

module.exports = nextConfig;